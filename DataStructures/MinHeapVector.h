#include <vector>

template<class dataType=int>
class MinHeapVector
{
public:
	void addElement(dataType element)
	{

	}
	bool addElement(dataType element)
	{

	}
private:
	std::vector<dataType> data;
	size_t size;
	size_t capacity;
};